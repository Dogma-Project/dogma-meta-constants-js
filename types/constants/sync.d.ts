export declare namespace Sync {
    enum SIZES {
        USER_ID = 5,
        NODE_ID = 5
    }
    enum Type {
        users = 0,
        nodes = 1,
        storage = 2,
        mail = 3,
        messages = 4,
        dht = 5
    }
    enum Action {
        get = 0,
        push = 1
    }
}
