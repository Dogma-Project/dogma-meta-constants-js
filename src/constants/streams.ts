export namespace Streams {
  export enum MX {
    handshake = 0,
    key = 1,
    test = 2,
    control = 3,
    messages = 4,
    mail = 5,
    dht = 6,
    web = 7,
    file = 8,
    relay = 9,
  }

  export enum SIZES {
    MX = 1,
    LEN = 2,
  }
}
